# 北疆秋色9日 · 旅行助手 需求文档 v0.3

## 1. 产品定位
单文件 HTML 手机 Web App，供同团 10–20 人出行前/中使用。打开即用，无需下载。
- 数据存储：浏览器 localStorage，按昵称隔离
- 部署：GitHub + Vercel（静态托管）
- 当前为纯前端 MVP，无后端

## 2. 核心功能

### 2.1 昵称入口
- 首次打开输入昵称；之后自动记住，直接进入
- 所有勾选/删除/备注按昵称隔离，互不影响

### 2.2 待办清单
**分类管理：**
- 顶部横向 tab 用于快速定位分类卡片，不隐藏其他卡片；「全部」回到页面最顶部。
- 默认分类为衣物、药物、证件钱物、其他物品、注意事项；支持自建分类。
- 默认分类及自建分类均可删除；删除分类不删除待办，原分类下的待办归入「未分类」。未分类为空时隐藏其 tab 和卡片。
- 空的默认／自建分类保留卡片，可选为移动目标；「未分类」作为兜底分类不可删除。

**待办项与修改分类交互（本轮定稿）：**
- 默认待办、自定义待办、未分类中的待办均可勾选、改分类、独立删除。
- 每条右侧为两个独立操作：「分类⌄」紧凑按钮与「×」删除按钮；点击二者不会误触勾选。
- 点击「分类⌄」后，菜单锚定在当前按钮附近，以视口级悬浮层覆盖后续列表，不占据文档流、不撑高当前卡片、无遮罩，也不吸底或居中弹窗；系统自动计算位置并限制在可视区内，避免被底部导航遮住。点击菜单外或再次点击可关闭。
- 菜单显示当前分类，当前项不可重复选择；选择其他默认分类、自定义分类或未分类后立即保存、关闭菜单，并提示“已移至某分类”。
- 菜单内支持「新建分类并移入」：原位展开名称输入框，点击「新建并移入」一次完成两项操作，不丢失当前待办。取消仅关闭输入框，不修改归类。
- 新分类名去除首尾空格、必填、最多20个字符；禁止与现有分类以及保留名称「全部／未分类」重名。名称作为文本安全展示。
- 自定义待办新增时可选所属分类；默认待办归类变更写入 catOverrides，自定义待办更新自身 cat。
- 内部以 default:<原待办ID> / custom:<稳定待办ID> 显式区分类型，不能按首字母 c 猜测或依赖数组下标。
- 修改只影响当前昵称；保持原勾选状态、其他待办、每日待办及备注不变。刷新后保留；存储失败应提示失败，不能显示保存成功。
- 读取旧版数据时补齐新增字段；所有状态变量完成初始化后才启动渲染。

### 2.3 每日行程（D1–D9）
**时间轴布局：**
- 左侧橙色竖线 + 圆点，右侧按时间顺序排列
- 每个节点：时间胶囊（清晨/上午/中午/下午/傍晚/晚上）+ 时段说明
- 地点自动带 📍 链接，点击跳高德 App 定位

**定位方式（高德 URI API）：**
- 有精确坐标的点：`https://uri.amap.com/marker?position=lng,lat&name=xxx&callnative=1`
- 无坐标的点：`https://uri.amap.com/search?keyword=xxx&city=新疆&callnative=1`
- 实现现状：坐标标记与关键词搜索并存，不代表每个点都已完成精确核验。本轮只修改分类交互，未重新核对地图数据。历史版本中景区／观景台共用坐标、区域地理编码等需在出发使用导航前单独核验。

**每天卡片：**
- 日期 + 标题（如 D2 · 9.25 周五 · 乌鲁木齐→赛里木湖）
- 车程、餐食说明
- 每日天气栏：显示当日主要停留地；有已查证高德预报时显示天气与温度，无覆盖时明确显示「预报尚未覆盖」及「出发前1–3天更新实际预报」，不得编造
- 时间轴 sections
- 酒店行（🏨 名称，点击跳高德）
- 当日自定义待办小栏（用户每天自己加/勾）
- 私人备注（textarea，按天存）

### 2.4 联系
- 领队春春 18382408097（一键拨号 tel:）
- 导游喜洋洋 15276633828（一键拨号 tel:）

### 2.5 集合信息（待办 tab 顶部）
- 集合时间 9.24 全天
- 集合酒店 伊力特酒店（乌鲁木齐火车南站德汇万达店）📍
- 天气（高德 MCP 查询，当前 9.24 多云 14–23℃，出发前 3 天再查最新）
- 装备提醒

## 3. 数据结构
localStorage key: `xj_state_<nickname>`
```
{
  checked: {},          // itemId -> true
  deletedItems: [],     // 已删除的默认 item id
  deletedCats: [],      // 已删除的分类 id
  catOverrides: {},     // 默认待办ID -> 新分类ID；缺省字段自动补空对象
  customCats: [],       // [{id, name, icon}]，按昵称持久化的自定义分类
  custom: [],           // [{id, cat, text, done}]；id 为稳定标识，不随排序／删除而改变
  daily: {},            // {Dn: [{id, text, done}]}
  notes: {}             // {Dn: "备注文本"}
}
```
当前昵称存 `xj_trip_nickname`。

## 4. 技术栈
- 单文件 index.html（无框架、无构建、无 CDN）
- 手机优先；页面跟随视口宽度，分类悬浮菜单最大宽280px，窄屏添加区域自动换行
- 主色：旅行橙 #E8833A；背景：米白 #FAF6F0
- 保留既有图标；修改分类箭头使用内联 SVG，无外部图标资源

## 5. 高德集成说明
- 跳转：使用高德公开 URI API（无需 API key）
- 坐标来源：高德 MCP maps_geo 地理编码
- 天气来源：此前高德 MCP 查询后手动写入的快照，非网页实时请求；D1 乌鲁木齐与 D2 赛里木湖邻近城市博乐显示已查证快照，D3–D9 当前超出已取得预报范围，先显示待更新状态。
- 注意：MCP 是 agent 侧工具，静态网页无法实时调用；天气和坐标都是硬编码进 HTML 的快照

## 6. 待办（后续迭代）
- 正式行程单到达后：替换 sections 的 time 字段为精确钟点、替换真实酒店名
- 天气：出发前 1–3 天重新查一次刷新
- 多人协作：当前 localStorage 单机，后续如需共享勾选状态需加后端
- 部署：本地验收完成后 push 到 Mona1Zhao/xinjiang-trip，Vercel 自动部署
