# 北疆秋色 9 日・旅行助手需求文档 v0.4（二期草案）

> 文档状态：本地开发与回归验证中，尚未提交或部署
> 对应线上版本：一期 v0.3 继续在线运行；二期仅在本地与 Supabase 测试环境验证
> 版本原则：二期持续维护本文件；保留《新疆旅游助手 - 需求文档.md》（v0.3），不再为每次讨论新建文档

## 1. 二期目标

在完整保留一期功能的基础上，实现个人数据跨设备云同步：



* 用户在任意设备输入已有的 “昵称 + 6 位安全码”，即可直接进入并恢复全部历史记录。

* 用户无需感知 “本机模式、迁移模式、恢复模式” 等技术概念。

* 不同同步身份的数据完全隔离。

* 不设置管理员角色，不提供查看或编辑他人数据的后台。

* 公共行程、酒店、天气、联系人继续随网站代码发布，不进入个人云数据。

* 网络不稳定时仍可使用；恢复网络后自动同步。

## 2. 已确认的产品决策



| 决策项        | 二期方案                             |
| ---------- | -------------------------------- |
| 云端后端       | Supabase Free                    |
| 用户入口       | 昵称 + 6 位数字安全码                    |
| 输入次数       | 每次新设备首次使用输入一次；成功后记住会话，日常再次打开无需重输 |
| 昵称规则       | 允许重名，不作为唯一身份                     |
| 唯一身份       | 昵称规范值 + 安全码校验后定位的随机 sync\_id     |
| 创建 / 恢复    | 用户不选择模式；服务端自动判断：组合已存在则恢复，不存在则创建  |
| 安全码确认      | 首次创建无需重复输入；提交后直接创建               |
| 忘记安全码      | 不提供找回；若旧设备仍有会话可继续使用，否则新建身份       |
| 管理员角色      | 不需要                              |
| 用户间可见性     | 完全隔离，只能访问自己的数据                   |
| 离线能力       | localStorage 作为本地缓存；云端为跨设备同步源    |
| 冲突策略       | 自动按单项更新时间合并，不向普通用户展示 “整份状态二选一”   |
| 同步码展示 / 导出 | 不支持导出；创建成功后不额外展示同步码卡片，也不提供再次查看明文 |

## 3. 范围边界

### 3.1 二期必须实现



1. 使用昵称 + 安全码统一完成创建或恢复。

2. 新设备成功验证后直接读取同一身份的全部历史数据。

3. 同步默认待办的勾选、删除和分类调整。

4. 同步自定义分类、自定义待办。

5. 同步 D1–D9 每日自定义待办和私人备注。

6. 保留本地缓存和离线编辑。

7. 自动处理多设备修改冲突。

8. 展示同步状态和异常恢复操作。

9. 从一期 localStorage 平滑迁移，不清空原数据。

### 3.2 二期暂不实现



* 邮箱、短信、微信或飞书登录。

* 安全码找回或管理员重置。

* 仅本机使用模式。

* 管理员后台或成员管理。

* 团员之间查看彼此待办。

* 公共待办协作、评论、聊天。

* 文件、图片或视频上传。

* 安全码导出、下载、二维码或明文再次查看。

* 公共行程、酒店、天气和联系人的云端编辑。

## 4. 用户与身份模型

二期只有一种角色：普通用户。



* 昵称可以重复。

* “昵称 + 安全码” 不是数据库主键，只用于服务端查找和验证身份。

* 服务端生成不可猜测的 `sync_id` 作为真实身份标识。

* 前端成功验证后保存短期会话凭证，不保存安全码明文。

* 用户只能读取和修改当前 sync\_id 的数据。

* 系统不提供用户搜索、昵称占用提示或其他身份列表。

### 4.1 同名用户

同一昵称可以对应多组不同安全码和不同 sync\_id。例如两位用户都叫 “小王”，只要安全码不同，数据互不影响。

为了防止服务端无法定位同名组合，恢复接口在服务端对该昵称的候选哈希进行受限校验；不得将候选数或是否存在返回给前端。

## 5. 统一进入流程

### 5.1 页面

首次进入或切换用户时，只显示两个字段：



* 昵称；

* 6 位数字安全码。

主按钮统一为 “进入我的行程”，不要求用户选择 “注册、登录、仅本机、恢复”。

辅助提示：

> 首次使用会自动创建；换设备输入相同昵称和安全码，即可恢复历史记录。安全码无法找回，请自行记住，且不要复用重要密码。

### 5.2 自动创建或恢复



1. 用户输入昵称和安全码。

2. 服务端按规范化昵称查找有限候选，并校验安全码哈希。

3. 若存在匹配身份：签发短期会话凭证，返回云端个人状态。

4. 若无匹配身份：创建新的 sync\_id，并使用本机已有的一期状态作为初始云端状态；本机无历史数据则创建默认状态。

5. 页面直接进入待办页，不再要求二次确认安全码或选择数据来源。

6. 成功后记住会话；同一设备再次打开直接进入并后台同步。

### 5.3 新设备体验



* 输入已有昵称和安全码。

* 验证成功后直接拉取云端状态并进入待办页。

* 目标体验：用户只感知 “数据还在”，不出现 “恢复身份、选择版本、覆盖本机” 等技术文案。

* 若新设备浏览器中存在另一个身份的本地缓存，不覆盖或删除该缓存；按当前 sync\_id 切换读取。

### 5.4 忘记安全码



* 不支持找回、导出或再次显示明文。

* 旧设备会话仍有效时，用户可以继续使用和同步，但仍不能查看明文安全码。

* 无任何有效设备会话时，只能使用新安全码创建一个新身份，无法取回旧云端数据。

* 页面不得暗示可联系管理员重置。

### 5.5 切换用户

一期 “切换” 升级为 “切换用户”：



1. 清除当前设备保存的会话凭证；

2. 不删除当前身份的云端数据；

3. 不删除其他 sync\_id 的本地缓存；

4. 返回昵称 + 安全码入口。

## 6. 同步体验

### 6.1 顶栏状态

昵称旁显示紧凑文字状态：



* `已同步`：最近一次拉取 / 上传成功；

* `同步中`：正在通信；

* `离线·待同步`：本地已修改，等待网络；

* `同步失败·重试`：请求失败，可点击重试。

不再出现 “仅本机”。状态不能只靠颜色表示。

### 6.2 本地优先编辑

待办勾选、分类调整、删除、每日待办和备注必须立即生效：



1. 先写 localStorage；

2. 为被修改的单项写入客户端更新时间；

3. 标记待同步；

4. 后台上传；

5. 成功后更新 `lastSyncedAt`；

6. 失败时保留本地修改并自动重试。

网络请求不得阻塞勾选或输入。

### 6.3 拉取时机



* 首次验证成功；

* 页面启动且已有有效会话；

* 页面从后台回到前台；

* 网络从离线恢复；

* 用户点击同步状态进行手动重试。

二期首版不要求 WebSocket 秒级推送，但设备重新进入或回到前台后应自动获取其他设备的最新修改。

## 7. 同步数据结构

云端为每个身份保存个人状态；公共行程不上传。



```
{

&#x20; "schemaVersion": 2,

&#x20; "checked": {

&#x20;   "cloth\_0": { "value": true, "updatedAt": "2026-09-22T00:00:00Z" }

&#x20; },

&#x20; "deletedItems": {

&#x20;   "cloth\_1": { "deleted": true, "updatedAt": "..." }

&#x20; },

&#x20; "deletedCats": {},

&#x20; "catOverrides": {

&#x20;   "cloth\_2": { "cat": "medicine", "updatedAt": "..." }

&#x20; },

&#x20; "customCats": \[

&#x20;   { "id": "...", "name": "摄影装备", "deleted": false, "updatedAt": "..." }

&#x20; ],

&#x20; "custom": \[

&#x20;   { "id": "...", "cat": "...", "text": "相机电池", "done": false, "deleted": false, "updatedAt": "..." }

&#x20; ],

&#x20; "daily": {

&#x20;   "D1": \[

&#x20;     { "id": "...", "text": "...", "done": false, "deleted": false, "updatedAt": "..." }

&#x20;   ]

&#x20; },

&#x20; "notes": {

&#x20;   "D1": { "text": "...", "updatedAt": "..." }

&#x20; },

&#x20; "updatedAt": "服务端时间",

&#x20; "revision": 1

}
```

不上传：公共行程、酒店、地图坐标、天气、联系人、设备名称等非必要数据。

## 8. 自动冲突合并

“整份状态二选一” 指的是让用户在 “整个云端版本” 和 “整个本机版本” 中选一个，会导致另一端的部分修改丢失。该方案不采用。

二期采用单项级自动合并：



* 默认待办勾选：按该待办 `updatedAt` 较新者生效。

* 分类调整：按该待办分类字段 `updatedAt` 较新者生效。

* 自定义分类、待办、每日待办：使用稳定 ID 合并；同一 ID 按最新时间更新。

* 删除：记录软删除标记和时间；删除时间晚于编辑时间时保留删除，否则保留编辑。

* 每日备注：每个 Dn 独立比较时间；同一天取更新时间较新的完整文本。

* 合并完成后提升 revision，再返回合并结果给当前设备。

* 同一毫秒极端冲突以服务端接收顺序为准。

普通用户不看到冲突弹窗；同步状态短暂显示 “已合并其他设备的更新”。

## 9. 一期数据迁移



* 二期首次进入时读取原 `xj_state_<nickname>`。

* 用户输入昵称 + 安全码后：


  * 匹配已有云身份：将本机旧状态与云端逐项合并；

  * 新建身份：将本机旧状态直接作为初始数据；

  * 本机无旧状态：使用默认空状态。

* 上传并回读一致前，不删除一期 localStorage。

* 云端空值不得覆盖非空本地数据。

* 迁移后继续保留本地缓存，作为离线副本。

## 10. Supabase 技术方案

### 10.1 实际架构

* Supabase PostgreSQL：保存同步身份、会话与个人状态。
* PostgreSQL `SECURITY DEFINER` RPC：完成身份创建 / 恢复、安全码校验、状态读取、revision 冲突检测与写入。
* Netlify：继续托管静态网页；验收后推送 `main` 才触发自动部署。
* localStorage：保存离线缓存与短期会话；安全码明文不保存。

首版没有使用 Edge Function。原因是当前能力可由受控 RPC 完成，减少额外部署与运行链路。前端只包含 Supabase 项目 URL 和 publishable key；不包含数据库密码或 `service_role` 密钥。

### 10.2 数据表

#### `sync_profiles`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 随机 `profile_id`，作为真实同步身份 |
| nickname_normalized | text | 规范化昵称，用于受限候选查询 |
| nickname_display | text | 显示昵称 |
| pin_hash | text | bcrypt 加盐哈希，禁止明文 |
| failed_attempts | integer | 预留的连续失败计数 |
| locked_until | timestamptz | 预留的临时锁定时间 |
| created_at / updated_at | timestamptz | 创建与更新时间 |

`nickname_normalized` 不设唯一约束，允许同名用户。

#### `user_states`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| profile_id | uuid | 对应 `sync_profiles.id`，唯一 |
| state | jsonb | 完整个人状态 |
| revision | bigint | 服务端乐观锁版本号 |
| updated_at | timestamptz | 服务端更新时间 |

#### `sync_sessions`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 会话记录 ID |
| profile_id | uuid | 只绑定一个同步身份 |
| token_hash | text | 会话 token 的 SHA-256 哈希 |
| expires_at | timestamptz | 30 天有效期 |
| created_at | timestamptz | 创建时间 |

三张表均启用 RLS，且撤销 anon / authenticated 对底表的直接权限。

### 10.3 RPC 接口

| RPC | 作用 |
| --- | --- |
| `enter_sync` | 昵称 + 安全码统一创建或恢复；返回 `profile_id`、会话 token、状态和 revision |
| `get_sync_state` | 使用会话 token 读取当前身份状态 |
| `put_sync_state` | 带基准 revision 写入；冲突时返回当前云端状态，由前端单项合并后重试 |
| `leave_sync` | 删除当前会话 token |

会话 token 仅在创建时返回明文；数据库只保存哈希。前端本地缓存按 `profile_id` 分键，避免同名不同安全码在同一设备串数据。

## 11. 安全规则

### 11.1 已实现

* 安全码使用 bcrypt 加盐哈希，数据库不保存明文。
* 前端不保存、记录或上报安全码明文；提交后立即清空输入框。
* 会话 token 随机生成，数据库只保存 SHA-256 哈希，30 天过期。
* 会话只绑定一个 `profile_id`。
* 三张底表启用 RLS，并撤销 anon / authenticated 的直接读写权限。
* 浏览器只能调用四个公开 RPC，不能直接查询身份表。
* 前端不包含数据库密码和 `service_role` key。

### 11.2 当前边界

* 昵称允许重复，因此“同名 + 未匹配安全码”会按产品规则创建新身份，而不是提示密码错误；输错一位安全码也可能意外创建新身份，但不会读到原身份数据。
* `failed_attempts` 和 `locked_until` 字段已预留，首版尚未启用连续失败锁定。
* 纯 PostgreSQL RPC 无法可靠取得客户端真实 IP，首版尚未实现 IP + 昵称限流；如正式公开给大量陌生用户，应迁移登录入口到 Edge Function 或 Netlify Function 后补限流、审计和滥用防护。
* 当前定位是小范围个人使用，不应将 6 位安全码复用于其他重要账号。

## 12. 展示与导出规则

“不支持导出” 表示：



* 不生成同步码文本文件、图片、二维码或下载包；

* 不提供复制同步码、再次查看明文同步码的页面；

* 不把安全码写进 PRD、日志、URL 或 localStorage。

“展示” 原本指创建后显示一张包含昵称和同步码提醒的凭证卡。现已取消：创建成功后只提示 “同步已开启，请记住你刚输入的安全码”，不重复展示用户输入的数字。

## 13. 异常与降级



| 场景           | 用户体验                      |
| ------------ | ------------------------- |
| 断网           | 允许进入最近缓存身份并编辑，显示 “离线・待同步” |
| 新设备首次使用且断网   | 无云端缓存，提示联网后进入             |
| Supabase 不可用 | 保留本地修改，后台重试               |
| 同名 + 未匹配安全码 | 按允许重名规则创建新身份；不读取原身份数据 |
| 频繁尝试 | 首版未限流；作为公开规模化前的待补安全项 |
| 多设备先后修改      | 自动逐项合并，提示已合并其他设备更新        |
| 浏览器缓存被清理     | 输入已有昵称 + 安全码恢复            |

## 14. 验收标准

### 14.1 无感跨设备



* 设备 A 首次输入昵称 + 安全码并使用。

* 设备 B 输入同一组合后直接进入，自动读取设备 A 的全部历史状态。

* 设备 B 后续再打开时无需重输。

* 入口不出现 “本机模式、注册、恢复、版本选择” 等技术概念。

### 14.2 身份隔离



* 相同昵称 + 不同安全码可创建独立身份。

* 错误安全码不能读取个人状态。

* 一位用户的修改不影响其他身份。

* 首版验收确认：同名 + 未匹配安全码不会读取已有身份数据，而会创建隔离的新身份。

* 连续错误限流或锁定列为公开规模化前的待补项。

### 14.3 数据同步



* 默认待办的勾选、删除、改分类可跨设备同步。

* 自定义分类、自定义待办可跨设备同步。

* D1–D9 每日待办和私人备注可跨设备同步。

* 页面刷新、关闭重开、设备切换后数据不丢失。

### 14.4 自动合并



* 设备 A 和 B 修改不同待办，两边改动都保留。

* 两设备修改同一待办时，更新时间较新的值生效。

* 同一天备注发生冲突时，较新的完整文本生效。

* 删除与编辑冲突按时间决定，不产生重复记录。

* 普通用户不需要选择整个云端或整个本机版本。

### 14.5 离线与迁移



* 断网时一期编辑功能正常。

* 恢复网络后自动上传待同步修改。

* 原一期 localStorage 可迁移，且迁移失败不会丢数据。

* 待办、行程、地图、天气、联系人功能无回归。

## 15. 开发与验证状态

### M1：Supabase 基础与身份隔离 — 已完成

* 已创建 `sync_profiles`、`user_states`、`sync_sessions`。
* 已创建四个 Security Definer RPC，并完成真实接口调用验证。
* 已验证同名不同安全码生成独立 `profile_id`，底表不可由浏览器直接读取。

### M2：无感进入与一期迁移 — 已完成

* 启动页已改为昵称 + 6 位安全码统一入口。
* 已实现自动创建 / 恢复、30 天会话、本地缓存按 `profile_id` 隔离。
* 只有真实存在的一期 `xj_state_<nickname>` 才参与迁移；全新设备直接采用云端状态，避免空状态覆盖历史。

### M3：单项同步与离线 — 已完成核心能力

* 待办、删除、分类、自定义分类、自定义待办、每日待办和每日备注均写入独立更新时间。
* 修改先落 localStorage，再延迟上传；回到前台或网络恢复时拉取。
* 顶栏支持“已同步 / 同步中 / 离线·待同步 / 同步失败·重试”。

### M4：冲突与回归 — 本地验证通过，待用户验收

* 已验证设备 A 勾选待办后，设备 B 使用同一昵称和安全码可恢复为 `1/30`。
* 已验证两个会话从同一 revision 修改不同待办：第二次写入返回冲突，前端合并重试后两项均保留，revision 从 1 增至 3。
* 已验证同一日备注取单项更新时间较新的完整文本。
* HTML 桌面与手机静态检查无脚本错误、资源错误或横向溢出。
* 二期已提交并推送 GitHub，Netlify 已完成生产部署。

## 15.1 2026-09-22 上线后补丁

* D1–D9 天气已更新为 2026-09-22 查询到的逐日预报；页面显示“天气预报·9月22日更新”，其中日期表示预报查询时间，不代表实际天气日期。
* 赛里木湖采用博乐参考，白哈巴采用哈巴河参考；页面明确提示山区体感、风力和夜间温度可能更低。
* D1 集合酒店和行程酒店统一使用准确高德坐标跳转。
* 登录页不预填昵称，也不提供默认安全码；用户必须自行输入昵称和 6 位安全码。
* 私人备注区增加“内容会自动保存，并同步到云端”的明确提示。

## 16. 当前结论

1. 强制使用云同步，不提供“仅本机使用”。
2. 安全码首次创建只输入一次，无二次确认；不导出、不复制、不再次展示明文。
3. 已实现单项级自动合并，不让用户选择整个云端或整个本机版本。
4. 二期核心跨设备链路已在本地和 Supabase 测试通过。
5. 当前适合小范围个人使用；公开规模化使用前，需要补充登录限流与防误输创建策略。
6. 保持一期 v0.3 文档不变，二期只持续维护本 v0.4 文档。

